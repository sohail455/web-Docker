sudo: required
services:
  - docker
before_install:
  - docker build -t sohailtalat/webapp -f Dockerfile.dev .
script:
  - docker run -e CI=true sohailtalat/webapp npm test
  - docker run -e CI=true sohailtalat/webapp npm run lint
  - docker run -e CI=true sohailtalat/webapp npm run build
